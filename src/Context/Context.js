import React from 'react';

export const Movie = React.createContext()

export const MovieProvider = Movie.Provider
export const MovieConsumer = Movie.Consumer


export const References = React.createContext()

export const ReferencesProvider = References.Provider
export const ReferencesConsumer = References.Consumer
